{% extends 'base.html.twig' %}

{% block title %}Éditer le menu
	{{ menu.name }}
{% endblock %}

{% block body %}
	<div class="d-flex flex-column align-items-center mt-4">
		<h1>Éditer le menu
			{{ menu.name }}</h1>

		{{ include('menu/_form.html.twig', {'button_label': 'Éditer'}) }}

		<a href="{{ path('app_admin_index') }}">Retour</a>
	</div>

	{# 
			Included in user/admin.html.twig
			{{ include('menu/_delete_form.html.twig') }} 
		#}
{% endblock %}
