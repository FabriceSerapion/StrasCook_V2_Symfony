{% extends 'base.html.twig' %}

{% block title %}Éditer la note
{% endblock %}

{% block body %}
	<div class="d-flex flex-column align-items-center mt-4">
		<h1>Éditer la note</h1>

		{{ include('user_rating/_form.html.twig', {'button_label': 'Éditer'}) }}

		<a href="{{ path('app_user') }}">Retour à votre espace privé</a>
	</div>
{% endblock %}
